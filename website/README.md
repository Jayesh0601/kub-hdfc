# Website files

Put your existing HTML folders here. If your repository already has `insurance`, `internetbanking`, `loans`, and `mobilebanking` at the root, keep them there and run the Docker build from the repository root.
